import React from 'react';
import './styles.css';

const App = () => {
    const env = process.env.NODE_ENV

    return (
        <div className=''>
            
        </div>
    );
};

export default App;