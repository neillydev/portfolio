import React from 'react';

const App = () => {
    const env = process.env.NODE_ENV

    return(
        <div>
            <h1 className="text-yellow-50">Hello world</h1>
        </div>
    )
};

export default App;